{
  "name": "@playroom/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "echo \"Run Drizzle migrations against Neon\""
  },
  "dependencies": {
    "express": "^4.18.4",
    "zod": "^3.23.0",
    "@playroom/db": "workspace:*"
  }
}
