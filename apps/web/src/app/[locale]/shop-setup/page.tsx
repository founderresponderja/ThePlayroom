export default function ShopSetupPage() {
  return (
    <div style={{ minHeight: '100vh', background: 'var(--bg)', padding: '2rem' }}>
      <h1 style={{ color: 'var(--text)' }}>🛍️ Configurar Sex Shop</h1>
      <p style={{ color: 'var(--text-muted)', marginTop: '0.5rem' }}>Em breve — onboarding Stripe Connect.</p>
    </div>
  )
}
